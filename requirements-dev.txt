# Dependencias de DESARROLLO. El proyecto se ejecuta sin ninguna (ver
# requirements.txt); estas hacen falta solo para trabajar sobre el.
#
#     python3 -m pip install -r requirements-dev.txt

# El runner de la suite. Los tests estan escritos con `unittest.TestCase` de la
# libreria estandar y pytest los ejecuta tal cual, asi que los dos comandos
# valen y quien no quiera instalar nada sigue teniendo `python3 -m unittest`.
pytest>=8

# Opcional, y solo para dibujar: la curva de aprendizaje de `train` y los
# paneles de `benchmark`. Sin el se avisa por el log y se sigue.
matplotlib
